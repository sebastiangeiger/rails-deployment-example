---
driver:
  name: vagrant

provisioner:
  name: ansible_playbook
  playbook: playbook.yml
  ansible_verbose: true
  ansible_verbosity: 3
  hosts: all

platforms:
  - name: ubuntu-14.04
    driver_config:
      box: ubuntu/trusty64

suites:
  - name: default
